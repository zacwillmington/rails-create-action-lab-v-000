<!-- Create your form here -->
<%= form_tag students_path do%>
    <label>First Name</label>
    <%= text_feild_tag :student[:first_name] %>
    <label>Last Name</label>
    <%= text_feild_tag :student[:last_name] %>
    <%= submit_tag, "Create Student" %>
<% end %>
