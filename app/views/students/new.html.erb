<!-- Create your form here -->
<%= form_tag(method: 'post')%>
    <label>First Name</label>
    <%= text_feild_tag :first_name %>
    <label>Last Name</label>
    <%= text_feild_tag :last_name %>
    <%= submit_tag, "Create Student" %>
<% end %>
